# Elden Ring 1.17.1 compatibility

Date: 2026-09-08. Target: standalone Summon.

Input executable: WW 2.7.1.0 at
`G:/SteamLibrary/steamapps/common/ELDEN RING/Game/eldenring.exe`.
SHA256: `1A3547101327F65D0C76DA2F9190AC0AA66871EA42BAE2AECC61E11A8B597891`.

The install path above was previously recorded as `F:/SteamLibrary/...`; that
drive no longer exists and the binary now resolves through the Steam library on
`G:`. The SHA256 is unchanged, so every measurement below still refers to the
same executable.

Re-ran binary-mapper against Summon's eae96df mapper profile: 107 entries,
102 unchanged, five changed. This profile has more entries than the HUD audit.
Generated local comparison: `target/compat-1171/mapper-comparison.json`.

| Entry | 1.17 | 1.17.1 |
| --- | --- | --- |
| register_task | 0xEB3DE0 | 0xEB3E50 |
| cs_phys_world_cast_ray | 0xC71D90 | 0xC71E00 |
| cs_ez_draw_draw_line | 0x26525D0 | 0x2652640 |
| cs_ez_draw_draw_triangle | 0x2652320 | 0x2652390 |
| cs_ez_draw_draw_wedge | 0x2651ED0 | 0x2651F40 |

Summon directly requires the corrected task registration entry. SpEffect add
and remove remain 0x3E8DC0 / 0x3EE2E0. The entire table must not be shifted.

Dependency fix is pinned to
`KamiyamaShiki0704/fromsoftware-rs@02fa5681e27fd2ecd9da79d34aef9fae96805539`,
on branch `codex/summon-er-1171`. It changes only version routing and five RVAs
relative to eae96df. Retains WW 2.7.0.0 and JP 2.7.0.1.

The HUD dependency branch has a different ChrIns tail layout. Summon deliberately
preserves its already validated 1.17 layout: total 0x580, debug_flags 0x538,
debug_role_param_id 0x54c. Regression tests enforce these values and creator
init-data size 0x100. These compile-time checks prevent a dependency regression;
they are not exhaustive verification of 1.17.1 live object memory.

Verification: dependency routing test passed; four Summon tests passed; locked
release build passed; PE is x64 and does not import VCRUNTIME/MSVCP dynamic CRT.
Cargo.lock changed only the four fsrs source references.

Deployed DLL SHA256:
`64BE9E4A576EF657A1940A8E753BD38E1BE731E99C14477414BDEE0A0345EBFB`.

Human checks pending: startup, first activation, requested animation, cleanup,
reused activation and changed player facing. JP 1.17.1 remains unsupported
until its executable is verified. No new Summon Release published yet.
